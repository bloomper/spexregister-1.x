= render :partial => 'administration/sub_menu'

- form_for :news, :url => news_index_url do |f|
  %fieldset
    %legend= t 'views.news.new_news'
    = render :partial => 'form', :locals => { :f => f }
    = render :partial => '/shared/new_resource_links', :locals => { :url => news_index_url }
