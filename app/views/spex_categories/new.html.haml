= render :partial => 'sub_menu'

- form_for :spex_category, :url => collection_url, :html => { :multipart => true } do |f|
  %fieldset
    %legend= t 'views.spex_category.new_spex_category'
    = render :partial => 'form', :locals => { :f => f }
    = render :partial => '/shared/new_resource_links'
