#header
  #logo
    = image_tag 'logo.png'
  #title
    %h1= t('views.base.title')
  #details
    - if current_user
      %ul#login-details
        %li
          = t 'views.base.logged_in_as'
          = current_user.username
        %li
          = link_to t('views.base.logout'), logout_path
  #progress-indicator
    = image_tag 'progress.gif'
    = t('views.base.loading')
