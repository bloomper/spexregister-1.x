- if current_user
  #menu
    %ul
      = tab :home, :accounts, :change_profile
      = tab :search, :searches, :advanced_searches
      = tab :administration, :users, :functions, :function_categories, :news, :spex, :spex_categories, :spexare
      = tab :help, :in_case_of_problems, :about
  #sub-menu
    = yield :sub_menu
