- if current_user
  #menu
    %ul
      = tab :home, :accounts, :profiles, :spexare, :activities, :memberships, :relationships
      = tab :search, :search, :advanced_search, { :route => 'new_search' }
      = tab :administration, :users, :functions, :function_categories, :news, :spex, :spex_categories
      = tab :help, :in_case_of_problems, :about
    %br.clear
  #sub-menu
    = yield :sub_menu
