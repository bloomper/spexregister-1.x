= render :partial => 'administration/sub_menu'

%h1= t 'views.function.page_title', :action => t('views.base.viewing_action')

%table.index
  %tr
    %th= t 'activerecord.attributes.function.name'
  %tr
    %td= h @function.name

%table.index
  %tr
    %th= t 'activerecord.attributes.function_category.name'
  %tr
    %td= h @function.function_category.name

= render :partial => '/shared/entity_details', :locals => {:entity => @function}
