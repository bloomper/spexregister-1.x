@options = { :action => 'view_link_item', :link_item_id => @link_item.id }
@update_options = @options.merge(:action => 'update_link_item')

page.remove element_row_id(@options)
page.insert_html :bottom, 'spexareItemLinksTBody', :partial => 'link_item', :locals => { :hidden => true }
page << "new TableRow.MoveAfter('#{element_row_id(@update_options)}', '#{element_row_id(@options)}');"
page.remove element_row_id(@update_options)
page.show element_row_id(@options)
page << "Spexregister.stripe('spexareItemLinksTable');"
